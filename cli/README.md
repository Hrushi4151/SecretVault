# SecretVault — Developer CLI (`secretvault`)

## Overview
The SecretVault CLI provides secure local secret injection (`secretvault run`), environment profiles, drift detection, and CI/headless authentication.

## Specification
For complete command architectures and runtime memory injection details, see [docs/CLI.md](../docs/CLI.md).
