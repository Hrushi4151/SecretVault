# SecretVault — VS Code Extension

## Overview
VS Code extension providing developers with environment variable auto-completion, secret reference linking, workspace status, and pre-commit secret leak warnings directly within the editor.
