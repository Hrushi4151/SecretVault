package com.secretvault.environment.entity;

/**
 * Standard environment types in SecretVault.
 * Defines the deployment tier of an environment.
 */
public enum EnvType {
    DEVELOPMENT,
    STAGING,
    PRODUCTION
}
