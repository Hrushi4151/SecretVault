package com.secretvault.access.model;

/**
 * Defines the hierarchical resource scope for permissions and access grants.
 */
public enum AccessScope {
    WORKSPACE,
    PROJECT,
    ENVIRONMENT,
    SECRET
}
